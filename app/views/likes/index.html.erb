<h2>あなたのいいね一覧</h2>

<% @liked_tweets.each do |tweet| %>
  <div>
    <p>投稿者: <%= tweet.user.uid %></p>
    <p>内容: <%= tweet.message %></p>
    <p>いいね数: <%= tweet.likes.count %></p>

    <%= button_to "いいね削除", like_path(tweet.id), method: :delete, data: { confirm: "本当に解除しますか？" } %>
  </div>
<% end %>
