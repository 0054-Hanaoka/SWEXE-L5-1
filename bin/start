#!/bin/bash
set -e

echo "=== DB schema load ==="
DISABLE_DATABASE_ENVIRONMENT_CHECK=1 bundle exec rails db:schema:load

echo "=== DB seed ==="
bundle exec rails db:seed

echo "=== Start Rails server with Thruster ==="
./bin/thrust ./bin/rails server -b 0.0.0.0 -p 10000
